services:
  ttrans:
    build: .
    image: ttrans:latest
    environment:
      TTRANS_HTTP_BIND: 0.0.0.0
    ports:
      - "127.0.0.1:47880:47880/tcp"
      - "44777:44777/udp"
    volumes:
      - ./docker-data:/data
    command: ["gui", "--http-port", "47880"]
